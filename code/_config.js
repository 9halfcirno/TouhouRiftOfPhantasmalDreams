let con = {
	"y_tilt": Math.PI / 5,
	"object2d_tilt": (Math.PI / 5),
	"tile_tilt": Math.PI / -2,
	
	"game_tick_interval": 1000 / 20,
	
	"camera_distance": 16,
	"camera_follow_speed": 1.5,
}

export {
	con as Config
}